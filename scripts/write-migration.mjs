import { writeFileSync } from "fs";

const lines = [
  "-- ============================================================",
  "-- Migration 014: Remove tenants table, use user_id everywhere",
  "-- ============================================================",
  "",
  "-- 1. Add order/business columns to users",
  "alter table public.users",
  "  add column if not exists business_name text,",
  "  add column if not exists order_method text not null default 'direct_chat' check (order_method in ('website', 'form', 'direct_chat')),",
  "  add column if not exists order_link text,",
  "  add column if not exists is_active boolean not null default true;",
  "",
  "-- 2. Migrate tenant data to users",
  "update public.users u",
  "set",
  "  business_name = t.name,",
  "  order_method  = coalesce(t.order_method, 'direct_chat'),",
  "  order_link    = t.order_link,",
  "  is_active     = t.is_active",
  "from public.tenants t",
  "where t.owner_id = u.id;",
  "",
  "-- 3. Tables that already have user_id",
  "alter table public.connected_pages drop constraint if exists connected_pages_tenant_id_fkey;",
  "drop index if exists idx_connected_pages_tenant;",
  "alter table public.connected_pages drop constraint if exists connected_pages_tenant_id_page_id_key;",
  "alter table public.connected_pages drop column if exists tenant_id;",
  "alter table public.connected_pages add constraint connected_pages_user_id_page_id_key unique(user_id, page_id);",
  "create index if not exists idx_connected_pages_user_id on public.connected_pages(user_id);",
  "",
  "alter table public.instagram_accounts drop constraint if exists instagram_accounts_tenant_id_fkey;",
  "drop index if exists idx_instagram_accounts_tenant;",
  "alter table public.instagram_accounts drop constraint if exists instagram_accounts_tenant_id_ig_account_id_key;",
  "alter table public.instagram_accounts drop column if exists tenant_id;",
  "alter table public.instagram_accounts add constraint instagram_accounts_user_id_ig_account_id_key unique(user_id, ig_account_id);",
  "create index if not exists idx_instagram_accounts_user_id on public.instagram_accounts(user_id);",
  "",
  "alter table public.whatsapp_accounts drop constraint if exists whatsapp_accounts_tenant_id_fkey;",
  "drop index if exists idx_whatsapp_accounts_tenant;",
  "alter table public.whatsapp_accounts drop constraint if exists whatsapp_accounts_tenant_id_phone_number_id_key;",
  "alter table public.whatsapp_accounts drop column if exists tenant_id;",
  "alter table public.whatsapp_accounts add constraint whatsapp_accounts_user_id_phone_number_id_key unique(user_id, phone_number_id);",
  "create index if not exists idx_whatsapp_accounts_user_id on public.whatsapp_accounts(user_id);",
  "",
  "alter table public.usage_logs drop constraint if exists usage_logs_tenant_id_fkey;",
  "drop index if exists idx_usage_logs_tenant;",
  "drop index if exists idx_usage_logs_tenant_created;",
  "drop index if exists idx_usage_logs_tenant_action_date;",
  "alter table public.usage_logs drop column if exists tenant_id;",
  "create index if not exists idx_usage_logs_user_id on public.usage_logs(user_id);",
  "create index if not exists idx_usage_logs_user_created on public.usage_logs(user_id, created_at desc);",
  "create index if not exists idx_usage_logs_user_action_date on public.usage_logs(user_id, action, created_at desc);",
  "",
  "-- 4. Tables that only have tenant_id",
  "alter table public.conversations add column if not exists user_id uuid;",
  "update public.conversations c",
  "set user_id = coalesce(",
  "  (select user_id from public.connected_pages where connected_pages.id = c.page_id limit 1),",
  "  (select user_id from public.instagram_accounts where instagram_accounts.id = c.instagram_id limit 1),",
  "  (select user_id from public.whatsapp_accounts where whatsapp_accounts.id = c.whatsapp_id limit 1),",
  "  (select owner_id from public.tenants where tenants.id = c.tenant_id limit 1)",
  ");",
  "alter table public.conversations alter column user_id set not null;",
  "alter table public.conversations add constraint fk_conversations_user_id foreign key (user_id) references public.users(id) on delete cascade;",
  "alter table public.conversations drop constraint if exists conversations_tenant_id_fkey;",
  "drop index if exists idx_conversations_tenant;",
  "drop index if exists idx_conversations_tenant_last_msg;",
  "alter table public.conversations drop column if exists tenant_id;",
  "create index if not exists idx_conversations_user_id on public.conversations(user_id);",
  "create index if not exists idx_conversations_user_last_msg on public.conversations(user_id, last_message_at desc);",
  "",
  "alter table public.ai_settings add column if not exists user_id uuid;",
  "update public.ai_settings a",
  "set user_id = coalesce(",
  "  (select user_id from public.connected_pages where connected_pages.id = a.page_id limit 1),",
  "  (select user_id from public.instagram_accounts where instagram_accounts.id = a.instagram_id limit 1),",
  "  (select owner_id from public.tenants where tenants.id = a.tenant_id limit 1)",
  ");",
  "alter table public.ai_settings alter column user_id set not null;",
  "alter table public.ai_settings add constraint fk_ai_settings_user_id foreign key (user_id) references public.users(id) on delete cascade;",
  "alter table public.ai_settings drop constraint if exists ai_settings_tenant_id_fkey;",
  "alter table public.ai_settings drop constraint if exists ai_settings_tenant_id_page_id_instagram_id_key;",
  "alter table public.ai_settings drop column if exists tenant_id;",
  "create unique index ai_settings_user_id_page_id_instagram_id_key on public.ai_settings(user_id, coalesce(page_id, '00000000-0000-0000-0000-000000000000'), coalesce(instagram_id, '00000000-0000-0000-0000-000000000000'));",
  "",
  "alter table public.knowledge_base add column if not exists user_id uuid;",
  "update public.knowledge_base kb set user_id = (select owner_id from public.tenants where tenants.id = kb.tenant_id);",
  "alter table public.knowledge_base alter column user_id set not null;",
  "alter table public.knowledge_base add constraint fk_knowledge_base_user_id foreign key (user_id) references public.users(id) on delete cascade;",
  "alter table public.knowledge_base drop constraint if exists knowledge_base_tenant_id_fkey;",
  "drop index if exists idx_knowledge_base_tenant;",
  "drop index if exists idx_knowledge_base_tenant_order;",
  "alter table public.knowledge_base drop column if exists tenant_id;",
  "create index if not exists idx_knowledge_base_user_id on public.knowledge_base(user_id);",
  "create index if not exists idx_knowledge_base_user_order on public.knowledge_base(user_id, sort_order);",
  "",
  "alter table public.orders add column if not exists user_id uuid;",
  "update public.orders o set user_id = (select owner_id from public.tenants where tenants.id = o.tenant_id);",
  "alter table public.orders alter column user_id set not null;",
  "alter table public.orders add constraint fk_orders_user_id foreign key (user_id) references public.users(id) on delete cascade;",
  "alter table public.orders drop constraint if exists orders_tenant_id_fkey;",
  "drop index if exists idx_orders_tenant;",
  "alter table public.orders drop column if exists tenant_id;",
  "create index if not exists idx_orders_user_id on public.orders(user_id);",
  "create index if not exists idx_orders_user_status on public.orders(user_id, status);",
  "",
  "-- 5. Drop tenants table",
  "drop trigger if exists tenants_updated_at on public.tenants;",
  "drop table if exists public.tenants;",
  "drop function if exists public.get_user_tenant_id();",
  "",
  "-- 6. Update handle_new_user trigger",
  "create or replace function public.handle_new_user()",
  "returns trigger as $$",
  "begin",
  "  insert into public.users (id, email, full_name, username, avatar_url)",
  "  values (",
  "    new.id,",
  "    new.email,",
  "    new.raw_user_meta_data->>'" + "'full_name'" + "',",
  "    new.raw_user_meta_data->>'" + "'username'" + "',",
  "    new.raw_user_meta_data->>'" + "'avatar_url'" + "'",
  "  );",
  "  return new;",
  "end;",
  "$$ language plpgsql security definer;",
  "",
  "-- 7. Update get_dashboard_stats",
  "drop function if exists public.get_dashboard_stats(uuid);",
  "create function public.get_dashboard_stats(p_user_id uuid)",
  "returns table (",
  "  total_pages bigint, total_instagram bigint, total_whatsapp bigint,",
  "  total_conversations bigint, total_messages bigint, ai_replies_today bigint,",
  "  quota_used integer, quota_daily integer",
  ") as $$",
  "begin",
  "  return query",
  "  select",
  "    (select count(*) from public.connected_pages where user_id = p_user_id and is_active = true),",
  "    (select count(*) from public.instagram_accounts where user_id = p_user_id and is_active = true),",
  "    (select count(*) from public.whatsapp_accounts where user_id = p_user_id and is_active = true),",
  "    (select count(*) from public.conversations where user_id = p_user_id and is_archived = false),",
  "    (select count(*) from public.messages m",
  "      join public.conversations c on c.id = m.conversation_id",
  "      where c.user_id = p_user_id),",
  "    (select count(*) from public.usage_logs",
  "      where user_id = p_user_id and action = 'ai_reply' and created_at >= current_date),",
  "    (select u.ai_quota_used from public.users u where u.id = p_user_id),",
  "    (select u.ai_quota_daily from public.users u where u.id = p_user_id);",
  "end;",
  "$$ language plpgsql stable;",
  "",
  "-- 8. Update get_platform_stats",
  "create or replace function public.get_platform_stats()",
  "returns table (",
  "  total_users bigint, total_pages bigint, total_instagram bigint,",
  "  total_conversations bigint, total_messages bigint,",
  "  ai_replies_total bigint, ai_replies_today bigint, total_tokens_used bigint",
  ") as $$",
  "begin",
  "  return query",
  "  select",
  "    (select count(*) from public.users),",
  "    (select count(*) from public.connected_pages where is_active = true),",
  "    (select count(*) from public.instagram_accounts where is_active = true),",
  "    (select count(*) from public.conversations where is_archived = false),",
  "    (select count(*) from public.messages),",
  "    (select count(*) from public.usage_logs where action = 'ai_reply'),",
  "    (select count(*) from public.usage_logs where action = 'ai_reply' and created_at >= current_date),",
  "    (select coalesce(sum(tokens_used), 0) from public.usage_logs);",
  "end;",
  "$$ language plpgsql stable security definer;",
  "",
  "-- 9. Drop old RLS policies",
  'drop policy if exists "Pages access for tenant owner" on public.connected_pages;',
  'drop policy if exists "Instagram accounts access for tenant owner" on public.instagram_accounts;',
  'drop policy if exists "Conversations access for tenant owner" on public.conversations;',
  'drop policy if exists "Messages access for tenant owner" on public.messages;',
  'drop policy if exists "Messages insert for tenant owner" on public.messages;',
  'drop policy if exists "Knowledge base access for tenant owner" on public.knowledge_base;',
  'drop policy if exists "AI settings access for tenant owner" on public.ai_settings;',
  'drop policy if exists "Usage logs access for tenant owner" on public.usage_logs;',
  'drop policy if exists "Orders access for tenant owner" on public.orders;',
  'drop policy if exists "WhatsApp accounts access for tenant owner" on public.whatsapp_accounts;',
  'drop policy if exists "Tenant access for owner" on public.tenants;',
  'drop policy if exists "Admins can update tenants" on public.tenants;',
  'drop policy if exists "Admins can read all tenants" on public.tenants;',
  "",
  "-- 10. Create new user_id-based policies",
  'create policy "Users can access own connected pages"',
  "  on public.connected_pages for all using (user_id = auth.uid());",
  'create policy "Users can access own instagram accounts"',
  "  on public.instagram_accounts for all using (user_id = auth.uid());",
  'create policy "Users can access own whatsapp accounts"',
  "  on public.whatsapp_accounts for all using (user_id = auth.uid());",
  'create policy "Users can access own conversations"',
  "  on public.conversations for all using (user_id = auth.uid());",
  'create policy "Messages select for own conversations"',
  "  on public.messages for select using (",
  "    exists (select 1 from public.conversations where conversations.id = messages.conversation_id and conversations.user_id = auth.uid())",
  "  );",
  'create policy "Messages insert for own conversations"',
  "  on public.messages for insert with check (",
  "    exists (select 1 from public.conversations where conversations.id = conversation_id and conversations.user_id = auth.uid())",
  "  );",
  'create policy "Users can access own knowledge base"',
  "  on public.knowledge_base for all using (user_id = auth.uid());",
  'create policy "Users can access own ai settings"',
  "  on public.ai_settings for all using (user_id = auth.uid());",
  'create policy "Users can view own usage logs"',
  "  on public.usage_logs for select using (user_id = auth.uid());",
  'create policy "Users can access own orders"',
  "  on public.orders for all using (user_id = auth.uid());",
];

writeFileSync("social-reply-ai/supabase/migrations/014_remove_tenants.sql", lines.join("\n"));
console.log("Migration written successfully!");
